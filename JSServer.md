# Summary of JS & Server work & SQL

<br></br>


## Javascript

- functional language
- no need to explicitly specify types
- extremely popular



```js

var a1 = // value that can be changed
let flag2 = // scope - blocked 
const my_constant = // value that cannot be changed

const number = "hello world";

function helloWorld() {
    console.log("Hello world"); // Output: hello world
}



// for, if, while work the same as Java.
for (let i = 0; i < 10 ; i++) {
    // rest of the code
}

while () {
    // rest of the code
}


// asynchronous functions
// Example to an asynchronous function:

// Use async, await keywords to create promises which can resolve or reject.
async function demo() {
    return await anotherFunction();
}





// Web development specific functions:

const demoObject = getElementById('demo'); // id="demo"
const demoObject2 = querySelector('.demo'); // class="demo"
const demoObject3 = querySelectorAll('.demo'); // class="demo", selects all the elements with such a selector. Returns an array of HTMLNode
const demoObject4 = getElementByClassName('demo'); // class="demo"



```

